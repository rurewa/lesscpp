1. Рассказать про именование переменных с показом картинки с тапками
2. Рассказать про биты и байты. Для самостоятельнього просмотра - ролики
3. Рассказать про std из книги и как делать в примерах
4. Объяснить про Long Long

Д.З. Уроки 33, 34, 35

План на среду:

1. Константы заглавными буквами
2. Конвертирование с помощью int и char без static_cost
3. Ещё раз про gitignore
4. Управляющие символы \n \t \v \" \' _ \\ \a 

Компилятор C++ игнорирует "пустые" строки, равно как и любое другое "пустое" пространство в тексте 'кода программы'.
 
	Все пустые строки, пробелы и перенос на новую строку нужен лишь для \\программиста\\, т. к. это повышает читаемость программы

1. Ещё раз про .gitignore и удалить из своих репозиториев все .bin
2. Рассказать про std из книги и как делать в примерах
3. Операторы ввода. Создание интерактивной программы. Cin, cin.ignore(), geline()
4. В качестве бонуса можно дать unistd.h и sleep

1. Ещё раз про .gitignore и удалить из своих репозиториев все .bin
2. Арифметика +, -, *, /, %. Целые и дробные числа.
3. В качестве бонуса можно дать unistd.h и sleep

1. Область видимости переменных. Виды переменных по видимости: глобальная (заглавные буквы), локальная, статическая.
2. В качестве бонуса можно дать вывод даннх в бинарном коде и с дизассемблером.

1. Создать шаблон программы на c++ и коприровать из него весь код для упрощения получения новых файлов
2. Комментарии по коду с простой арифметикой - объявление и инициализация переменных в строку, очистка переменной, правила ассоциативности
3. Операторы сравнения <, > , <=, >=, ==, != и bool

Д.З. Урок 41

1. Инкремент - пост и преинкремент
2. Операторы сравнения.

1. Операторы сравнения и bool

20.11
1. Команда stat 
1. Логические операторы: &&, ||, !=
2. Сравнение с помощью тернарного оператора.

Урок №37, №46, №44

1. Несколько задач по арифметике
2. Нахождение большего их 3-х чисел

Д.З. 
1. Создать простую интерактивную программу для перевода сантиметров в дюймы. В программе должна быть задержка вывода результата на 1 секунду.
2. Переделать таблицу истинности тем, кому я сделал замечания.
3. Написать интерактувную программу сравнения 2-х натуральных чисел с помощью тернарного оператора.
4. Написать интерактувную программу нахождения последней цифры натурального числа с помощью оператора остатка от деления.

Д.З. 30.12.2023
1. Папки и порядок в репозиториях.
2. Добавить setprecision() и unistd - у кого ещё нет
3. sizeof(bool) - упрежнение по основным типам данных
4. Арифметические и логические задачи: среднее арифметическое, поездка на машине (bool) - 2-а варианта.

2. Решение арифметических и логических задач.

Д.З.
1. Навести порядок в своём репозитории для c++. Создать тематические папки с названиями, например: Math, Logic, Var, InputOutput, Delay, DataType,Score, Ternar и др. Распределеить все имеющиеся в репозитории файлы программ по этим папкам;
2. Написать интерактивную программу, которая принимает от пользователя 3-и натуральных числа, и возвращает их среднее арифметическое
3. Написать интерактивную программу, которая вычисляет исправность сервера при 2-х условиях: исправность кондиционера и кулера процессора. Серевер исправен только если оба условия true. 
Пример: 

Кондиционер работает? 1
Кулер работает? 1
Исправность сервера: true

Кондиционер работает? 1
Кулер работает? 0
Исправность сервера: false

04.12.2022
1. Добавить в README.md cout << setprecision(5);
2. Задача с автомобилем 2
3. Задача про плавание в бассейне
4. Задача человека с зонтом
5. Формальная логика

11.01.2023
1. > в Readme.md у всех
2. Заставить всех написать простую программку с выводом результата в файл.
3. Do...While

План по фиксированным массивам
1. Простые массивы int, char, float/double, string. Константы
2. Объявление - примеры. Положительные и отрицательные элементы.
3. Инициализация (заполнение) ручная и счётчиком
4. Размер массива в байтах и количество в нём элементов
5. Добавление в массив - не возможно
6. Удаление из массива - не возможно
7. Сортировки элементов - прямая, обратная, нахождение дубликатов, отрицательные чисел
8. Арифметика в массиве

План обучения на 22.01.2023https://gitflic.ru/project/rurewa/education/blob?file=UbuntuPumping%2Fscan_lanhttps://gitflic.ru/project/rurewa/education/blob?file=UbuntuPumping%2Fscan_lan.md&branch=master.md&branch=master
1. Векторный массив
2. Защита от некорректного пользовательского ввода
3. Указатели и ссылки с реальными примерами
4. Динамический массив

План обучения на 18.06.2023 для начинающих
1. Проверить домашку
2. Переменные: целочисленные (short, int, long, signed-unsigned). Локальные и глобальные. Область видимости.

План обучения на 21.06.2023 для начинающих
1. Проверить домашку (в тетрадках)
2. Переменные: целочисленные (short, int, long, signed-unsigned). Локальные и глобальные. Область видимости.

План обучения на 25.06.2023 для начинающих
1. Тип char и последовательный вывод символов
2. Локальные переменные и глобальные переменные, область видимости - разница

План обучения на 28.06.2023 для начинающих

1. Домашка по переписыванию стандартной структуры программы c++
2. Домашка по тренировки методу слепой печати
3. Присваивание и инициализация переменных. Область видлимости. Перестановка значений 2-х переменных.

План обучения на 02.07.2023 для начинающих

1. Арифметика в C++: вещественные числа.
2. Арифметика в C++: символы и строки.

